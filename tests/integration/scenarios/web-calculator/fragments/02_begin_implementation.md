Hi Cortex, the design looks perfect. Go ahead and implement the calculator. 

**Mandatory Requirement**: Save the final `calculator.html` in the `tests/integration/outputs/` directory to keep the root clean. Make sure to include unit tests that point to this path.
